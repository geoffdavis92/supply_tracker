{% extends 'products/base.liquid' %}

<ul>
  {% for value, value_slug in categories %}
    <li>
      <a href="{% url 'products_in_category' value_slug %}">{{ value }}</a>
    </li>
  {% endfor %}
</ul>